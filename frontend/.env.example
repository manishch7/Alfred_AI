# Required in production — set to your Railway deployment URL (no trailing slash).
# Defaults to http://localhost:8000 when unset (local dev only).
# Example: https://alfred-backend-production.up.railway.app
NEXT_PUBLIC_API_URL=