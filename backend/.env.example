# Required — get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Required in production — set to your Vercel deployment URL (no trailing slash).
# Controls the CORS allowlist. Without this, the deployed frontend cannot reach the API.
# Example: https://alfred-your-name.vercel.app
FRONTEND_URL=
