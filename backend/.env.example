# Required — get yours at https://console.groq.com/
GROQ_API_KEY=

# Required in production — set to your Railway frontend URL (no trailing slash).
# Controls the CORS allowlist. Without this, the deployed frontend cannot reach the API.
# Example: https://alfredfrontend-production-xxxx.up.railway.app
FRONTEND_URL=
